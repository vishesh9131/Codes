
# # file_path = vg.generate_random_graph(40)
# file_path = vg.generate_random_graph(40)

# adj_matrix = np.loadtxt(file_path, delimiter=",")
# strong_relations, top_nodes = vg.find_top_nodes(adj_matrix)
# vg.draw_graph(adj_matrix, set(range(len(adj_matrix))), top_nodes)
# print(top_nodes)
